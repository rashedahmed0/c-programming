#include <stdio.h>
#include <math.h>

int main()
{
    // Qs.7 (a) write a program print the average of 3 number
    int x = 10;
    int y = 5;
    int z = 15;

    printf("%d", (x + y + z) / 3);

    return 0;
}
