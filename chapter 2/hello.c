#include <stdio.h>
#include <math.h>

int main()
{
    // Qs.5 Write a program if number divided by 2 or not
    int x;
    printf("enter number : ");
    scanf("%d", &x);
    printf("%d \n", x % 2 == 0);

    return 0;
}
