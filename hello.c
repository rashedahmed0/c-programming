#include <stdio.h>

int main()
{

    // format specefires
    int age = 24;
    printf("age is %d \n", age);

    float pi = 3.1416;
    printf("pi value is %f \n", pi);

    char star = '*';
    printf("star look like %c \n", star);

    return 0;
};